:author: https://github.com/wangzhaohe/swot-learning
:source-highlighter: pygments
:icons: font
:font-size: 16px
:scripts: cjk
:stem: latexmath
:experimental:
:toc:
:toc: right
:toc-title: 目录
:toclevels: 4
:tip-caption: ⚡
:note-caption: ❕
:important-caption: ❗
:caution-caption: ⚠️
:warning-caption: ‼️

= 学习 Svelte

// 只有当后端不是 pdf 时才包含以下内容
ifndef::backend-pdf[]
++++
<style>
/* 1. 默认状态：初始就是隐藏的 */
#toc {
    max-width: 0 !important;
    opacity: 0 !important;
    padding-left: 0 !important;
    padding-right: 0 !important;
    border: none !important;
    overflow: hidden !important; /* 必须在这里，保证收缩时内容不溢出 */
    white-space: nowrap !important;

    /* 【关键】：过渡动画必须放在基类里，这样“展开”和“收回”都有动画 */
    transition: max-width 0.3s cubic-bezier(0.4, 0, 0.2, 1),
                padding 0.3s cubic-bezier(0.4, 0, 0.2, 1),
                opacity 0.3s ease;
}

/* 2. 展开状态：点击后添加 .active 或 .expanded 类 */
#toc.active {
    max-width: 530px !important;
    opacity: 1 !important;
    padding: 10px !important; /* 根据需要恢复 padding */
    overflow-y: auto !important;
}

/* 3. 列表样式保持不变 */
#toc ul, #toc li {
    margin-bottom: 8px !important;
    line-height: 1.4 !important;
}
</style>

<button id="toggleButton">展开目录</button>
<div id="toc">
    </div>

<script>
const toggleButton = document.getElementById('toggleButton');
const contentDiv = document.getElementById('toc');

toggleButton.addEventListener('click', () => {
    // 切换 active 类
    const isExpanded = contentDiv.classList.toggle('active');

    // 逻辑：如果现在是 expanded (true)，说明打开了，按钮应该提示“收起”
    toggleButton.textContent = isExpanded ? '收起目录' : '展开目录';
});
</script>
++++
endif::[]

== 创建 svelte 项目
https://svelte.dev/docs/svelte/getting-started

[source,console]
----
npx sv create 项目名
cd myapp
npm install
npm run dev
----

== .svelte 文件
https://svelte.dev/docs/svelte/svelte-files

一个 .svelte 文件的核心功能可以总结为：它是构建 Svelte 应用的基本单元，将组件的逻辑、内容和样式整合在一个文件中，形成完整的、可复用的 UI 模块。

实际上就是一个项目组件。

== .svelte.js / .svelte.ts
https://svelte.dev/docs/svelte/svelte-js-files

除了具有普通的 js/ts 文件的功能外，还可以使用 svelte 的 runes 符文魔法。

== runes 魔法符文
魔法符文是用在文件 .svelte and .svelte.js / .svelte.ts 中的，用来控制编译器。

魔法符文是以 $ 开始关键字，像是一个函数的调用。如下：

    let message = $state('hello');

特点：

1. 自动导入，无需手动 import
2. 魔法符文本身不能赋值给一个变量，也不能被作为参数传递给函数。
3. 如果用错了，编译器会进行提示。

=== $state()
https://svelte.dev/docs/svelte/$state

https://svelte.dev/tutorial/svelte/state

创建响应式状态，让 UI 跟着变化。

[source,javascript]
----
<script>
	  let count = $state(0);
</script>

<button onclick={() => count++}>
	  clicks: {count}
</button>
----

==== Deep state 默认是深度响应式
深度状态：Deep state

$state 会为数组和普通对象创建“深度响应式代理”（deeply reactive state proxy）。

解构会丢失响应性：如果从响应式对象中解构出属性，解构出的变量将只是普通值，不再具有响应性。比如下面代码中的 done 和 text 都##不具响应性##了。

[source,javascript]
----
let { done, text } = todos[0];

// this will not affect the value of `done`
// 下面再赋值，也无法使 done 成了响应式的变量，这点要注意一下
todos[0].done = !todos[0].done;

// 网页中再这样使用就不行了 <p>{done}</p>
----

==== Classes 实例没有响应式
https://svelte.dev/docs/svelte/$state#Classes

类实例化以后的对象们不是响应式的。参考 Classes验证.adoc

箭头函数的防止 this 丢失的应用

===== $state 用在 class 里的响应式验证
本篇验证 Svelte 5 官方文档 https://svelte.dev/docs/svelte/$state#Classes 中的写法：

*class 实例本身不会被 proxy 代理，但可以把 `$state` 用在 class 字段声明，或构造函数里对属性的首次赋值上。*
比如该例子中的 done 和 text 两个属性的定义就是这样的。下面我们进行测试。

环境：Svelte `^5.56.1`，`vite.config.js` 已强制开启 `runes: true`，Vitest（server + client 两个项目）。

====== 1. 官方示例（后面要验证 $state 的写法）
.@edit src/lib/state-class/Todo.svelte.js
[source,javascript]
----
/* global $state */

/**
 * 验证 https://svelte.dev/docs/svelte/$state#Classes 中的写法。
 *
 * 官方说明：class 实例本身**不会**被 proxy 代理，但可以分别在
 *  1. class 字段声明处（public / private 都可）
 *  2. 构造函数里对某个属性的**首次**赋值处
 * 使用 `$state`，从而让单个属性具备响应式。
 *
 * 注意：文件名必须是 `*.svelte.js`，模块里的 runes 才会被编译。
 */
export class Todo {
    /** 字段声明处使用 $state */
    done = $state(false);

    /**
     * @param {string} text
     */
    constructor(text) {
        /** 构造函数中对该属性的首次赋值处使用 $state */
        this.text = $state(text);
    }

    /** 把两个响应式字段一起重置 */
    reset() {
        this.text = '';
        this.done = false;
    }
}
----

====== 2. 验证用的文件
为支持 runes，类定义必须放在 `.svelte.js` 后缀的模块里。共新增 7 个文件：

[cols="2,3"]
|===
| 文件 | 作用

| `src/lib/state-class/Todo.svelte.js`
| 官方示例原样（JSDoc 注释）

| `src/lib/state-class/tracker.svelte.js`
| 用 `$effect.root` 订阅属性的辅助函数，用于证明「真的响应式」

| `src/lib/state-class/Todo.svelte.spec.js`
| 单元 + 响应式测试（6 个）

| `src/lib/state-class/edge-cases.svelte.js`
| private 字段（`#done = $state(false)`）示例

| `src/lib/state-class/edge-cases.svelte.spec.js`
| private 字段响应式 / 解构失效（2 个）

| `src/lib/state-class/edge-cases.spec.js`
| 编译期合法性与报错校验（4 个）

| `src/routes/state-class/+page.svelte`
| 可视化页面 `/state-class`

| `src/routes/state-class/page.svelte.spec.js`
| 页面交互测试（5 个）
|===

[NOTE]
====
1. 页面测试不能叫 `+page.svelte.spec.js` —— `+` 是 SvelteKit 保留前缀，会让 `svelte-kit sync` 直接失败。
2. 改用同目录下的 `page.svelte.spec.js`，依然命中 client 项目的 include glob `src/**/*.svelte.{test,spec}.{js,ts}`。
====

====== 监听器定义
.src/lib/state-class/tracker.svelte.js
[source,javascript]
----
/**
 * https://share.gemini.google/4Do68CR3eWIS gemini 说明该代码
 * `$effect` 在 node/SSR 编译产物里是空实现，所以响应式断言必须跑在 client（浏览器）项目。`tracker.svelte.js` 在 `$effect.root` 里订阅一个 getter
 * runes 只能在 `.svelte` / `.svelte.js` 文件里使用，所以这个辅助函数单独放在 `.svelte.js` 中。
 * tracker.svelte.js = 一个可挂在任意 getter 上的响应式监听器 + 计数器。
 * runs 计数不动,说明被观察的东西没有响应式;
 * runs 计数随修改增加,说明它是货真价实的 $state 响应式属性。
 * 它是你验证 "class 里 $state 用对没有" 的测试利器。
 */

/* global $effect */
import { flushSync } from 'svelte';

/** @param {() => unknown} getter 读取待观察属性的函数 */
export function createTracker(getter) {
    /** @type {unknown} */
    let value = undefined; // 保存最近一次读到的值
    let runs = 0;          // effect 被执行的次数

    // 1. 开启一个不受组件生命周期限制的全局副作用监听
    const destroy = $effect.root(() => {
        $effect(() => {
            value = getter();  // 告诉 $effect 要观察的值，比如 done/text
            runs += 1;         // 只要值变了， effect 就会重跑，次数 +1
        });
    });

    // 2. 强制立即同步执行一次，确保初始状态下的 value 和 runs 有值 (runs 变成 1)
    flushSync();

    return {
        /** effect 最近一次读到的值 */
        get value() {
            return value;
        },
        /** effect 被执行了多少次 */
        get runs() {
            return runs;
        },
        /**
         * 修改状态并同步 flush，确保 effect 已经重跑
         * @param {() => void} mutate
         */
        run(mutate) {
            flushSync(mutate);
        },
        destroy
    };
}
----

====== Todo 类测试
.src/lib/state-class/Todo.svelte.spec.js
[source,javascript]
----
import { describe, expect, it } from 'vitest';
import { Todo } from './Todo.svelte.js';
import { createTracker } from './tracker.svelte.js';

// 单元 + 响应式测试（6 个）
// 注意：这个文件的名字是 *.svelte.spec.js，会跑在 vitest 的 client（浏览器）项目里。
// 原因是 Svelte 在 node/SSR 编译产物中 $effect 不会执行，只有客户端才能真正验证响应式。
// 同理，Todo.svelte.js / tracker.svelte.js 必须带 .svelte.js 后缀，runes 才会被编译。

describe('Todo：把 $state 用在 class 里', () => {
    it('字段声明处的 $state(false) 初始值正确', () => {
        const todo = new Todo('learn svelte');
        expect(todo.done).toBe(false);
    });
    it('构造函数首次赋值处的 $state(text) 初始值正确', () => {
        const todo = new Todo('learn svelte');
        expect(todo.text).toBe('learn svelte');
    });
    it('修改 done（字段声明式 $state）会触发响应式更新', () => {
        const todo = new Todo('learn svelte');
        const tracker = createTracker(
            () => todo.done
        );

        expect(tracker.value).toBe(false);
        expect(tracker.runs).toBe(1);

        tracker.run(() => {
            todo.done = true;
        });

        expect(todo.done).toBe(true);
        expect(tracker.value).toBe(true);
        expect(tracker.runs).toBe(2);
    });
    it('修改 text（构造函数式 $state）会触发响应式更新', () => {
        const todo = new Todo('learn svelte');
        const tracker = createTracker(
            () => todo.text
        );

        expect(tracker.value).toBe('learn svelte');

        tracker.run(() => {
            todo.text = 'learn runes';
        });

        expect(todo.text).toBe('learn runes');
        expect(tracker.value).toBe('learn runes');
        expect(tracker.runs).toBe(2);
    });
    it('reset() 同时重置两个字段，并各自触发更新', () => {
        const todo = new Todo('learn svelte');
        const doneTracker = createTracker(() => todo.done);
        const textTracker = createTracker(() => todo.text);

        doneTracker.run(() => {
            todo.done = true;
            todo.text = 'changed';
        });

        expect(doneTracker.value).toBe(true);
        expect(textTracker.value).toBe('changed');
        expect(doneTracker.runs).toBe(2);
        expect(textTracker.runs).toBe(2);

        textTracker.run(
            () => todo.reset()
        );

        expect(todo.text).toBe('');
        expect(todo.done).toBe(false);
        expect(textTracker.value).toBe('');
        expect(doneTracker.value).toBe(false);
        expect(textTracker.runs).toBe(3);
        expect(doneTracker.runs).toBe(3);
    });
    it('实例本身没有被 proxy：后加的普通属性不具备响应式', () => {
        /**
         * note 是后加的普通属性，就是为了测试它不具备响应式
         * @type {Todo & { note?: string }}
         */
        const todo = new Todo('learn svelte');
        const tracker = createTracker(
            () => todo.note
        );

        expect(tracker.runs).toBe(1);

        tracker.run(() => {
            todo.note = 'not reactive';  // 此处可以随便给字符串
        });

        // 值确实写进去了，但 effect 没有重跑 —— 说明实例不是 proxy 的响应式
        expect(todo.note).toBe('not reactive');
        expect(tracker.runs).toBe(1);  // 如果 note 是响应式，就会触发 $effect
    });
});
----

====== 定义类的私有属性
.#edit src/lib/state-class/PrivateTodo.svelte.js
[source,javascript]
----
/* global $state */

/**
 * private 字段（`#done = $state(false)`）示例
 * 文档里说 `$state` 可以用在 class 字段上，「无论是 public 还是 private」。
 * 这里验证 private（`#` 私有字段）的写法，并通过 getter / setter 暴露。
 */
export class PrivateTodo {
    #done = $state(false);  // 私有属性

    /** @param {string} text */
    constructor(text) {
        this.text = $state(text);
    }

    get done() { return this.#done; }

    /** @param {boolean} value */
    set done(value) { this.#done = value; }

    reset() {
        this.text = '';
        this.done = false;
    }
}
----

====== 测试类私有属性响应
.#file src/lib/state-class/PrivateTodo.svelte.spec.js
[source,javascript]
----
import { describe, expect, it } from 'vitest';
import { PrivateTodo } from './placement-rules.svelte.js';
import { createTracker } from './tracker.svelte.js';

// private 字段响应式 / 解构失效（2 个）
// 跑在 vitest 的 client（浏览器）项目里：只有客户端编译产物中 $effect 才会真正执行。

describe('边界情况', () => {
    it('private 字段 #done = $state(false) 同样具备响应式', () => {
        const todo = new PrivateTodo('learn svelte');
        const tracker = createTracker(
            () => todo.done
        );

        expect(todo.done).toBe(false);
        expect(tracker.runs).toBe(1);

        tracker.run(() => {
            todo.done = true;
        });

        expect(todo.done).toBe(true);
        expect(tracker.value).toBe(true);
        expect(tracker.runs).toBe(2);
    });
    it('解构出来的值只是一份快照，不再具备响应式', () => {
        const todo = new PrivateTodo('learn svelte');
        const { text } = todo;
        const tracker = createTracker(
            () => text
        );

        expect(text).toBe('learn svelte');

        tracker.run(() => {
            todo.text = 'changed';
        });

        // 实例上的字段变了，但之前解构出来的局部变量不会跟着变
        expect(todo.text).toBe('changed');
        expect(text).toBe('learn svelte');
        expect(tracker.runs).toBe(1);
    });
});
----

====== 服务端编译测试
.#file src/lib/state-class/placement-rules.spec.js
[source,javascript]
----
/**
 * 这个用例跑在 server（node）项目里：只做编译期校验，不需要 $effect，也就不需要浏览器。
 * 编译期合法性与报错校验（4 个）
 */

import { compileModule } from 'svelte/compiler';
import { describe, expect, it } from 'vitest';

/**
 * 按 client 模式编译一段源码，返回报错信息；没有报错则返回 null。
 * @param {string} source
 */
function compileError(source) {
    try {
        compileModule(source, {
            generate: 'client',
            filename: 'case.svelte.js'
        });
        return null;
    } catch (error) {
        return /** @type {Error} */ (error).message;
    }
}

describe('$state 在 class 里的合法 / 非法位置', () => {
    /**
     * 两种写法：作为属性和构造方法的首次赋值都正确
     * 注意 $state 是作为字符串存在的
     */
    it('文档里的两种写法都能正常编译', () => {
        const message = compileError(`
            export class Todo {
                done = $state(false);
                constructor(text) {
                    this.text = $state(text);
                }
            }
        `);
        expect(message).toBeNull();
    });
    // 注意 $state 是作为字符串存在的
    it('private 私有字段也能正常编译', () => {
        const message = compileError(`
            export class Todo {
                #done = $state(false);
            }
        `);
        expect(message).toBeNull();
    });
    // 注意 $state 是作为字符串存在的
    it('构造函数里「非首次」赋值会编译报错', () => {
        const message = compileError(`
            export class Todo {
                constructor(text) {
                    this.text = text;
                    this.text = $state(text);  // 这不是首次赋值
                }
            }
        `);
        expect(message).toContain(
            'Cannot assign to a state field before its declaration'
        );
    });
    // 注意 $state 是作为字符串存在的
    it('在普通方法里用 $state 赋值会编译报错', () => {
        const message = compileError(`
            export class Todo {
                addNote() {
                    this.note = $state('x'); // addNote 是一个普通的方法，不能用 $state
                }
            }
        `);
        // 只能在类的构造器中使用 $state
        expect(message).toContain(
            'the first assignment to a class field at the top level of the constructor'
        );
    });

    /**
     * 只有三种情况可以使用 $state:
     * `$state(...)` can only be used as a variable declaration initializer, a class field declaration, or the first assignment to a class field at the top level of the constructor.
     */
});
----

====== Todo 页面定义
.#file src/routes/state-class/+page.svelte
[source,html]
----
<script>
import { Todo } from '$lib/state-class/Todo.svelte.js';

/** class 实例组成的数组：数组本身被 proxy，实例本身不会 */
let todos = $state([
    new Todo('读 Svelte 文档'),
    new Todo('写验证代码')
]);

// 用一句话理解：doneCount 是一个"会自动重新计算的数字"，只要跟它相关的数据一变，Svelte 就会自动重算，然后模板里用到它的地方自动更新。
const doneCount = $derived(
    todos.filter((todo) => todo.done).length
);  // 计算 todos 已完成个数

let draft = $state('');  // 定义新增待办响应式变量

/** @param {SubmitEvent} event */
function addTodo(event) {
    /* 不阻止浏览器的默认行为会发生什么
        浏览器的默认动作是：把表单字段序列化，然后导航到 action 指定的 URL——这里没有 action，就导航到当前页面地址（/state-class?）。
        结果是一次整页刷新，后果是：
            todos、draft 这些 $state 全部被重置，刚加的待办没了；
            地址栏会多出一个 ?；
            页面会闪一下（重新走一次 SSR/ hydration
    */
    event.preventDefault();

    const text = draft.trim();
    if (text === '')  // 没有输入内容则退出函数
        return;

    todos.push(new Todo(text));
    draft = '';
}
</script>
<svelte:head>
    <title>$state in class 验证</title>
</svelte:head>

<main class="mx-auto max-w-xl space-y-6 p-8">
    <div class="space-y-2">
        <h1 class="text-2xl font-bold">$state 用在 class 里</h1>
        <p class="text-sm text-gray-600">
            已完成
            <strong data-testid="done-count">
                {doneCount} / {todos.length}
            </strong>
        </p>
    </div>
    <form onsubmit={addTodo} class="flex gap-2">
        <input
            bind:value={draft}
            data-testid="draft-input"
            placeholder="添加一条待办"
            class="flex-1 rounded border px-3 py-2"
        />
        <button type="submit"
                data-testid="add"
                class="rounded bg-blue-600 px-4 py-2 text-white">
            添加
        </button>
    </form>
    <ul class="space-y-2">
        {#each todos as todo, index (todo)}
            <li class="flex items-center gap-3 rounded border px-3 py-2">
                <input type="checkbox" 
                       bind:checked={todo.done}
                       data-testid="todo-done-{index}" 
                />
                <input
                    bind:value={todo.text}
                    data-testid="todo-input-{index}"
                    class="flex-1 rounded border px-2 py-1"
                />
                <span
                    data-testid="todo-text-{index}"
                    class:text-gray-400={todo.done}
                    class:line-through={todo.done}
                >
                    {todo.text}
                </span>
                <button
                    type="button"
                    data-testid="reset-{index}"
                    onclick={() => todo.reset()}
                    class="rounded border px-2 py-1 text-sm"
                >
                    reset()
                </button>
            </li>
        {/each}
    </ul>
    <p class="text-sm text-gray-500">
        勾选 checkbox 或修改文本，上方内容立即变化，说明「字段声明处」和「构造函数里」的
        <code>$state</code> 都生效了。
    </p>
</main>
<style>
</style>
<!--
可视化页面 `/state-class`
-->
----

====== Todo 页面测试
.#file src/routes/state-class/page.svelte.spec.js
[source,javascript]
----
// 注意：不能叫 +page.svelte.spec.js —— SvelteKit 保留 `+` 前缀，会让 svelte-kit sync 直接失败。
// 去掉 + 之后依然匹配 vitest client 项目的 include：`src/**/*.svelte.{test,spec}.{js,ts}`

import { page } from 'vitest/browser';
// describe: 分成组进行测试
// it: 相当于 test，是个测试单元
// expect: 期望的值
import { describe, expect, it } from 'vitest';
import { render } from 'vitest-browser-svelte';
import Page from './+page.svelte';

describe('/state-class 页面', () => {
    it('初始渲染出两条待办', async () => {
        render(Page);

        await expect.element(page.getByTestId('done-count')).toHaveTextContent('0 / 2');
        await expect.element(page.getByTestId('todo-text-0')).toHaveTextContent('读 Svelte 文档');
        await expect.element(page.getByTestId('todo-text-1')).toHaveTextContent('写验证代码');
    });
    it('勾选 checkbox 后计数实时更新（class 字段声明处的 $state 生效）', async () => {
        render(Page);

        await page.getByTestId('todo-done-0').click();
        await expect.element(page.getByTestId('done-count')).toHaveTextContent('1 / 2');
    });
    it('编辑输入框会同步更新展示（构造函数里的 $state 生效）', async () => {
        render(Page);

        await page.getByTestId('todo-input-0').fill('读 Svelte 5 runes 文档');
        await expect
            .element(page.getByTestId('todo-text-0'))
            .toHaveTextContent('读 Svelte 5 runes 文档');
    });
    it('点击 reset() 会清空文本并取消勾选', async () => {
        render(Page);

        await page.getByTestId('todo-done-0').click();
        await page.getByTestId('todo-input-0').fill('临时内容');
        await expect.element(page.getByTestId('done-count')).toHaveTextContent('1 / 2');

        await page.getByTestId('reset-0').click();
        await expect.element(page.getByTestId('todo-input-0')).toHaveValue('');
        await expect.element(page.getByTestId('todo-done-0')).not.toBeChecked();
        await expect.element(page.getByTestId('done-count')).toHaveTextContent('0 / 2');
    });
    it('新增待办后仍然具备响应式', async () => {
        render(Page);

        await page.getByTestId('draft-input').fill('跑一遍测试');
        await page.getByTestId('add').click();

        await expect.element(page.getByTestId('done-count')).toHaveTextContent('0 / 3');
        await expect.element(page.getByTestId('todo-text-2')).toHaveTextContent('跑一遍测试');

        await page.getByTestId('todo-done-2').click();

        await expect.element(page.getByTestId('done-count')).toHaveTextContent('1 / 3');
    });
});
----

====== 3. 验证结论


====== 两种写法都生效
[cols="2,3,1"]
|===
| 写法 | 行为 | 结论

| `done = $state(false)`（字段声明）
| 改 `todo.done` 时 effect 重跑（runs 1 → 2）
| ✅ 响应式生效

| `this.text = $state(text)`（构造函数首次赋值）
| 改 `todo.text` 时 effect 重跑
| ✅ 响应式生效

| `#done = $state(false)`（private 字段）
| 编译为 `$.state(false)` + `$.get/$.set`，effect 重跑
| ✅ 也生效

| 调用 `reset()` 后两个字段仍响应式
| 两个 effect 在重置后各自再重跑
| ✅ 生效
|===

====== 实例确实没有被 proxy
给实例后加一个普通属性，值写进去了，但 effect **没有**重跑——这正是文档说的「只对显式包了 `$state` 的字段生效」：

[source,js]
----
/** @type {Todo & { note?: string }} */
const todo = new Todo('learn svelte');
const tracker = createTracker(() => todo.note);

tracker.run(() => {
    todo.note = 'not reactive'; // 写进去了
});

expect(todo.note).toBe('not reactive');
expect(tracker.runs).toBe(1); // effect 没重跑 => 实例不是 proxy
----

// 注意前面已经有这个例子了

====== 解构会丢失响应式
[source,js]
----
const { text } = todo;          // 只是快照
const tracker = createTracker(() => text);

tracker.run(() => { todo.text = 'changed'; });

expect(todo.text).toBe('changed');
expect(text).toBe('learn svelte'); // 局部变量不会跟着变
expect(tracker.runs).toBe(1);
----

// 注意前面已经有这个例子了

====== 4. 首次赋值是编译期强制的
通过 `compileModule(source, { generate: 'client' })` 直接编译源码得到验证：

[cols="2,3"]
|===
| 写法 | 编译结果

| 文档里的两种写法 / private 字段
| 正常编译（无报错）

| 构造函数里先 `this.text = text` 再 `this.text = $state(text)`
| 报错 `Cannot assign to a state field before its declaration`

| 在普通方法里 `this.note = $state('x')`
| 报错 `state_invalid_placement`（消息要求写在构造函数顶层对字段的首次赋值）
|===

====== 5. 页面可视化
手工对网站进行测试 `npm run dev` 后访问 `/state-class`：

    勾选 checkbox、编辑输入框、`reset()`、新增待办都会实时更新 DOM。

使用工具自动对网站进行测试 `page.svelte.spec.js` 用 Playwright 覆盖了这些行为。

====== 6. 运行结果
[source,console]
----
$ npm run test
Test Files  6 passed (6)
     Tests  19 passed (19)
----

[source,console]
----
$ npm run build     # 通过
$ npm run check     # 新增文件零类型错误
----

NOTE: 本机首次运行 `npm run test` 需要先 `npx playwright install chromium`（client 项目依赖浏览器；既有的 `Welcome.svelte.spec.js` 也同样需要）。

====== 7. 小结
* 文档里的 `Todo` 类写法**完全正确**。

* `$state` 可放在：class 字段声明（public 或 private）、构造函数里对属性的**首次**赋值。

* 实例不被 proxy：只有显式用 `$state` 包装的字段才有响应式；后加属性、解构出来的变量都不具备响应式。

* 「首次赋值」由编译器强制，写错位置会直接报编译错误。

==== 内置响应式类
https://svelte.dev/docs/svelte/$state#Built-in-classes

==== $state.raw 禁止深度响应式


==== $state.snapshot 从响应式变量中提取快照，给第三方库或者api传值用


==== $state.eager 乐观更新UI


==== Passing-state-into-functions
https://svelte.dev/docs/svelte/$state#Passing-state-into-functions

.用类的方式来实现传递响应式到函数中
[source,javascript]
----
class Adder {
    #input;

    constructor(input) {
        this.#input = input;
    }

    get value() {
        return this.#input.a + this.#input.b;
    }
}

let input = $state({ a: 1, b: 2 });
let total = new Adder(input);
console.log(total.value); // 3

input.a = 3;
input.b = 4;
console.log(total.value); // 7
----

==== Passing state across modules 
https://svelte.dev/docs/svelte/$state#Passing-state-across-modules

参考官网的例子，可选为

1. 导出响应式的对象
+
[source,javascript]
----
// This is allowed — since we're updating
// `counter.count` rather than `counter`,
// Svelte doesn't wrap it in `$.state`
export const counter = $state({
	count: 0
});

export function increment() {
	counter.count += 1;
}
----

2. 只导出操作的函数，不导出响应式
+
[source,javascript]
----
let count = $state(0);

export function getCount() {
	return count;
}

export function increment() {
	count += 1;
}
----

=== $derived()    包含简单的表达式
[source,javascript]
----
<script>
	let count = $state(0);
	let doubled = $derived(count * 2);
</script>

<button onclick={() => count++}>
    {doubled}
</button>

<p>{count} doubled is {doubled}</p>
----

==== $derived.by() 包含更复杂的语句


==== Overriding derived values 乐观更新UI
https://svelte.dev/docs/svelte/$derived#Overriding-derived-values

==== Deriveds and reactivity
https://svelte.dev/docs/svelte/$derived#Deriveds-and-reactivity

需要注意 $derived() 也可能会影响 $state 定义的变量内容。

[source,javascript]
----
let items = $state([ /*...*/ ]);

let index = $state(0);
let selected = $derived(items[index]);
----

==== Destructuring
$derived 被解构后，解构出来的变量，依然是响应式的。

.这个例子更容易理解
[source,html]
----
<script>
    let price = $state(100);
    let quantity = $state(2);
    let discount = $state(0.9);

    let {
        total,
        count,
        finalPrice
    } = $derived({
        total: price * quantity,
        count: quantity,
        finalPrice: price * quantity * discount
    });
</script>

<p>商品数量：{count}</p>
<p>原价：{total}</p>
<p>折后价：{finalPrice}</p>

<button onclick={() => quantity++}>
    增加一个
</button>

<button onclick={() => discount -= 0.1}>
    再打九折
</button>
----

=== $effect
https://svelte.dev/docs/svelte/$effect

Effects are functions that run when state updates, and can be used for things like calling third-party libraries, drawing on <canvas> elements, or making network requests. They only run in the browser, not during server-side rendering.
写在 $effect 中可以被监测到的响应式变量有更新时，$effect函数中的代码就会运行。可以应用到调用第三方库、画画、做网络访问请求。
$effect 函数只会运行在浏览器中，不会运行在 SSR 服务端。

Generally speaking, you should not update state inside effects, as it will make code more convoluted and will often lead to never-ending update cycles. If you find yourself doing so, see when not to use $effect to learn about alternative approaches.
通常来讲，在 $effect 中不要去更新被它监测的变量，因为会特别复杂，也会导致死循环。一旦你这样做了，就去看看 https://svelte.dev/docs/svelte/$effect#When-not-to-use-$effect

举例如下：$effect 监测了 color and size，更改 size or color，则会执行 $effect 函数中的语句
[source,html]
----
<script>
    let size = $state(50);
    let color = $state('#ff3e00');

    let canvas;

    $effect(() => {
        const context = canvas.getContext('2d');
        context.clearRect(0, 0, canvas.width, canvas.height);

        // this will re-run whenever `color` or `size` change
        context.fillStyle = color;  // 监测 color
        context.fillRect(0, 0, size, size);  // 监测 size
    });
</script>

<canvas bind:this={canvas} width="100" height="100"></canvas>

<div>
    <label>
        大小: 
        <input type="range" min="10" max="100" bind:value={size} />
        {size}
    </label>
</div>

<div>
    <label>
        颜色: 
        <input type="color" bind:value={color} />
    </label>
</div>
----

==== Understanding lifecycle 
Your effects run after the component has been mounted to the DOM, and in a microtask after state changes. Re-runs are batched (i.e. changing color and size in the same moment won’t cause two separate runs), and happen after any DOM updates have been applied.

$effect 函数的执行在组件挂载之后，而且是在监测的响应式变量更新以后在微任务中执行。比如同时更改了 color and size，$effect 中的代码是在 DOM 更新后批量执行的，不会分开执行。

You can use $effect anywhere, not just at the top level of a component, as long as it is called while a parent effect is running.

$effect 不单单是只能在组件的顶层去使用，它在一个已有的 $effect 内部调用也是可以的，但是不能独立在函数或条件分支中直接调用。

.不能用的例子
[source,html]
----
<script>
	let count = $state(0);

	function setupEffect() {
		// ❌ 错误！没有父级 effect 在运行
		$effect(() => {
			console.log(count);
		});
	}

	setupEffect(); // 这里调用会报错
</script>
----

了解 teardown function 函数，在清理副作用时很有用。

==== Understanding dependencies 
$effect automatically picks up any reactive values ($state, $derived, $props) that are **synchronously** read inside its function body (including indirectly, via function calls) and registers them as dependencies. When those dependencies change, the $effect schedules a re-run.

$effect 监测的 **同步** 运行的响应式变量们，在变化时，$effect 函数中的代码会重新运行。

If $state and $derived are used directly inside the $effect (for example, during creation of a reactive class), those values will not be treated as dependencies.

直接在 $effect 中使用 $state and $derived 定义的变量，就不能被当作 $effect 的依赖。

Values that are read asynchronously — after an await or inside a setTimeout, for example — will not be tracked. Here, the canvas will be repainted when color changes, but not when size changes (demo):

异步读取的响应式变量，也不会成为依赖。如下例 color 是依赖，size 还是依赖。

[source,javascript]
----
$effect(() => {
    const context = canvas.getContext('2d');
    context.clearRect(0, 0, canvas.width, canvas.height);

    // this will re-run whenever `color` changes...
    context.fillStyle = color;

    setTimeout(() => {
        // ...but not when `size` changes
        context.fillRect(0, 0, size, size);
    }, 0);
});
----

An effect only reruns when the object it reads changes, not when a property inside it changes. (If you want to observe changes inside an object at dev time, you can use $inspect.)

[source,html]
----
<script>
    let state = $state({ value: 0 });
    let derived = $derived({ value: state.value * 2 });

    // this will run once, because `state` is never reassigned (only mutated)
    $effect(() => {
        state;
    });

    // this will run whenever `state.value` changes...
    $effect(() => {
        state.value;
    });

    // ...and so will this, because `derived` is a new object each time
    $effect(() => {
        derived;
    });
</script>

<button onclick={() => (state.value += 1)}>
    {state.value}
</button>

<p>{state.value} doubled is {derived.value}</p>
----

An effect only depends on the values that it read the last time it ran. This has interesting implications for effects that have conditional code.

注意 $effect 能够监测响应式变量并重新运行的条件是：$effect 上次运行时读取了哪些响应式变量。

For instance, if condition is true in the code snippet below, the code inside the if block will run and color will be evaluated. This means that changes to either condition or color will cause the effect to re-run.

例如，上一次执行 $effect 函数时，condition 是 true 时，condition 和 color 都会被监测到。

Conversely, if condition is false, color will not be evaluated, and the effect will only re-run again when condition changes.

相反地，如果 condition 是 false，color 就不能被监测到，只有当 condition 变化时，$effect 才会重新运行。

[source,javascript]
----
import confetti from 'canvas-confetti';

let condition = $state(true);
let color = $state('#ff3e00');

$effect(() => {
    if (condition) {
        confetti({ colors: [color] });
    } else {
        confetti();
    }
});
----

==== $effect.pre
https://svelte.dev/docs/svelte/$effect#$effect.pre

$effect.pre 为你提供了在 DOM 更新流程中介入的“前置钩子”，让你有机会在变化发生前读取 DOM 旧状态，从而做出更精确的、与布局相关的操作。

比如这个官方的例子展示的是可以用到聊天工具的对话中，如微信聊天时当前对话来了新的消息，滚动条在屏幕底部附近时直接滚动到最底部，若滚动条在前面看历史记录时，则滚动条不动。

在绝大多数情况下，应该使用标准的 $effect，它会在 DOM 更新之后运行。只有在极少数需要在浏览器绘制新帧之前，读取或操作即将更新的 DOM 状态时，才需要使用 $effect.pre。

.这个例子只是展示打印先后而已
[source,javascript]
----
<script>
    let count = $state(0);
    let div = $state();

    $effect.pre(() => {
        console.log('🟡 [pre] DOM 文本:', div?.textContent || '未挂载');
        console.log('🟡 [pre] count 状态:', count);
    });

    $effect(() => {
        console.log('🟢 [普通] DOM 文本:', div?.textContent || '未挂载');
        console.log('🟢 [普通] count 状态:', count);
    });
</script>

<div bind:this={div}>Count: {count}</div>
<button onclick={() => count++}>增加</button>
----

==== $effect.tracking
https://svelte.dev/docs/svelte/$effect#$effect.tracking

高级工具，95% 情况用不到。

==== $effect.pending
直接在 svelte5 中集成了实现骨架屏或者加载中功能，比 vue 要使用 nuxt 的 useFetch 更加直观和方便。

==== $effect.root
在这儿使用过 src/lib/state-class/tracker.svelte.js

=== $props
1. 功能是实现父子组件之间的传值，一般都是从父组件传值给子组件。

2. 子组件解构时定义的 callback valus 不是响应式的。

3. 子组件解构变量时可以重新命名变量。

4. 可以使用 rest 收集剩余的参数。

5. 父组件传入的数据变化时，子组件接收到的数据也会跟着变化。

6. 子组件改变父组件传入的数据时分情况

    6.1 父组件传入的是普通对象，子组件的更改不会有响应式的效果。
    6.2 父组件传入的是响应式数据，子组件的更改会有响应式的效果，但是会报警告说子组件修改的数据不是子组件自己的。
    6.3 子组件接收 props 时定义的默认值 fallback 没有响应式的效果。
    6.4 除非使用了 $bindable 才可以双向绑定。

7. props 是可以写类型安全的。 https://svelte.dev/docs/svelte/$props#Type-safety

==== $props.id
文档：https://svelte.dev/docs/svelte/$props#$props.id()

作用: 生成一个**在当前组件实例内唯一且稳定**的 id，主要用于表单元素的可访问性（`label` 和 `input` 的 `for`/`id` 配对）。

基本用法

[source,html]
----
<script>
  const uid = $props.id();
</script>

<label for="{uid}-firstname">First Name:</label>
<input id="{uid}-firstname" type="text" />

<label for="{uid}-lastname">Last Name:</label>
<input id="{uid}-lastname" type="text" />
----

**要点：调用一次，存到变量，用后缀拼出多个 id。**

为什么需要它

**核心价值：同一组件被渲染多次时，避免 id 冲突。**

```svelte
<NameForm />
<NameForm />
```

- 写死 `id="firstname"` → 页面上出现重复 id，`<label>` 全指向第一个 input，可访问性坏掉
- 用 `$props.id()` → 每个实例拿到不同 id（如 `svelte-1a2b3c`、`svelte-4d5e6f`），互不干扰

关键结论

. 组件会被多次实例化（可复用组件、表单等），有意义，刚需 
. 组件在整个应用中只会渲染一次，写死字符串也行

补充

- SSR 场景下，服务端和客户端需要生成**一致的 id** 才能 hydration 成功，`$props.id()` 内部会处理。
- **不能**用 `Math.random()` 之类的代替，会破坏 hydration。

一句话记忆: 它不是为了"在一个组件里区分多个 id"，而是为了"**让同一组件的不同实例之间 id 不冲突**"。

=== $bindable
通常父子组件之间的数据流向是从父组件到子组件。

不要经常使用，会使用数据流不可预测，也会使组件难以维护。但确实可以简化代码，小心使用吧。

使用了 $bindable 就可以在子组件中去更改父组件传入的响应式变量了。

.App.svelte 父组件
[source,html]
----
<script>
    import FancyInput from './FancyInput.svelte';

    let message = $state('hello');
</script>

<FancyInput bind:value={message} />
<p>{message}</p>
----


.FancyInput.svelte 子组件
[source,html]
----
<script>
    let { value = $bindable(), ...props } = $props();
</script>

// input 处的 bind 是标签 input 自己的，和本例无关
<input bind:value={value} {...props} />

<style>
    input {
        font-family: 'Comic Sans MS';
        color: deeppink;
    }
</style>
----

=== $inspect 打印监测的响应式变量
在开发模式使用，在生产模式下会被编译成空操作。

.监测响应式变量
[source,html]
----
<script>
    let count = $state(0);
    let message = $state('hello');

    $inspect(count, message); // will console.log when `count` or `message` change
</script>

<button onclick={() => count++}>Increment</button>
<input bind:value={message} />
----

==== $inspect(...).with 替代 console.log
可以自定义操作，取代 console.log()

[source,javascript]
----
<script>
    let count = $state(0);

    $inspect(count).with((type, count) => {
        if (type === 'update') {
            debugger; // or `console.trace`, or whatever you want
        }
    });
</script>

<button onclick={() => count++}>Increment</button>
----

==== $inspect.trace(...) 查看哪个变量引起的副作用
查看是哪个变量导致的 $effect 被执行了

[source,javascript]
----
<script>
    import { doSomeWork } from './elsewhere';

    $effect(() => {
        // $inspect.trace must be the first statement of a function body
        $inspect.trace();
        doSomeWork();
    });
</script>
----

=== $host
可以打包成 web component 组件，供其他 js 框架使用。比如让 react / vue3 / 纯js 都可以调用。

$host 可以让你写的 web components 去和宿主进行事件沟通。

可以参考官网的例子：https://svelte.dev/docs/svelte/$host

== Template Syntax


=== Basic markup
https://svelte.dev/docs/svelte/basic-markup

==== tags


===== 小写字母就是普通的 html 标签
.src/routes/template-syntax/tags/lower/+page.svelte
[source,html]
----
<div>小写字母就是普通的 html 标签</div>
<button>按钮</button>
----

===== 大写字母开头是组件--定义组件
.src/routes/template-syntax/tags/capitalised/+page.svelte
[source,html]
----
<script>
    import Widget from './Widget.svelte';
</script>

<div>
    <Widget />
</div>
----

===== 大写字母开头是组件--使用组件
.src/routes/template-syntax/tags/capitalised/Widget.svelte
[source,html]
----
<div>
    大写字母开头的标签会被当作组件，而不是普通的 html 标签
</div>
----

===== 点号命名空间的方式
以点号分隔的是组件，点号前面的一般是命名空间，后面是组件名。

官方机制是**命名空间导入**：`import * as my from '...'` 得到的是包含多个组件的对象，模板里 `<my.stuff />` 的点号触发“组件”判定。我按这个思路在 `dot` 目录下建一套完整示例。

现在运行项目自带的类型检查，验证点号组件的写法确实能被编译通过。
示例已创建并通过编译校验。访问路径：**`http://localhost:5173/template-syntax/tags/dot`**

[discrete]
===== 目录结构

```
src/routes/template-syntax/tags/dot/
├── +page.svelte   ← 页面（路由入口）
├── my.js          ← 命名空间模块（关键）
├── Stuff.svelte   ← 组件 stuff
└── Thing.svelte   ← 组件 thing
```

[discrete]
===== 实现思路

**1. 命名空间模块 `my.js`** — 用对象把多个组件聚合成一个“命名空间”：

```js
export { default as stuff } from './Stuff.svelte';
export { default as thing } from './Thing.svelte';
```

**2. 页面用「命名空间导入」拿到这个对象**：

```src/routes/template-syntax/tags/dot/+page.svelte
<script>
	// 命名空间导入：my 是一个对象，my.stuff / my.thing 是其中的组件
	import * as my from './my.js';
</script>
```

**3. 模板里用点号访问**：

```src/routes/template-syntax/tags/dot/+page.svelte
	<my.stuff />
	<my.thing />
```

[discrete]
===== 核心原理

Svelte 判定一个标签是「组件」还是「HTML 元素」只看标签名：

- 首字母**小写**且**不含点号**（如 `<div>`）→ 普通 HTML 元素；
- 首字母**大写**（如 `<Widget>`）**或含点号**（如 `<my.stuff>`）→ 组件。

所以 `<my.stuff>` 里的 `my` 必须是**作用域内一个真实存在的对象**，`.stuff` 是它的一个成员、且该成员是一个组件。`import * as my from './my.js'` 正好返回这样“命名空间对象”，点号前面的 `my` 就是命名空间，后面的 `stuff` 就是组件名——和 ES 模块的命名空间语义完全一致。

[discrete]
===== 校验结果

运行 `svelte-check` 结果：新增的 4 个文件**零错误零警告**，`<my.stuff />` / `<my.thing />` 这种点号写法编译通过。

> 备注：若你更想要单文件自包含的写法，也可以不建 `my.js`，直接在页面里 `import Stuff from './Stuff.svelte'; const my = { stuff: Stuff };`，效果一样；但“命名空间”这一概念用 `import * as my` 表达最贴切，所以我选了当前方案。

====== 点号命名空间的方式--使用点号的页面
.src/routes/template-syntax/tags/dot/+page.svelte
[source,html]
----
<script>
	// 命名空间导入：my 是一个对象，my.stuff / my.thing 是其中的组件
	import * as my from './my.js';
</script>

<div class="space-y-3">
	<h1 class="text-2xl font-bold">点号（命名空间）组件</h1>
	<p class="text-sm text-gray-600">
		点号写法 <code>&lt;my.stuff /&gt;</code> 中，点号前是命名空间，点号后是组件名。小写标签如
		<code>&lt;div&gt;</code> 是普通 HTML 元素，而带点号的标签一定是组件。
	</p>
	<my.stuff />
	<my.thing />
</div>
----

====== 点号命名空间的方式--命名空间模块
.src/routes/template-syntax/tags/dot/my.js
[source,javascript]
----
/**
 * 命名空间模块。
 *
 * 页面里写 `import * as my from './my.js'` 之后，`my` 就是这个模块导出的对象。
 * 于是模板中可以写 `<my.stuff />`、`<my.thing />`：
 *   点号前面（my）是命名空间，点号后面（stuff / thing）是组件名。
 *
 * 关键点：只要标签里出现点号（或首字母大写），Svelte 就会把它当作“组件”而不是普通 HTML 标签。
 */
export { default as stuff } from './Stuff.svelte';
export { default as thing } from './Thing.svelte';
----

====== 点号命名空间的方式--命名空间模块下的组件 Stuff
.src/routes/template-syntax/tags/dot/Stuff.svelte
[source,html]
----
<div class="rounded border border-emerald-500 bg-emerald-50 px-3 py-2">
	我是命名空间 <code>my</code> 下的组件 <code>stuff</code>，由 <code>&lt;my.stuff /&gt;</code> 渲染
</div>
----

====== 点号命名空间的方式--命名空间模块下的组件 Thing
.src/routes/template-syntax/tags/dot/Thing.svelte
[source,html]
----
<div class="rounded border border-sky-500 bg-sky-50 px-3 py-2">
	我是同一个命名空间下的另一个组件 <code>thing</code>，由 <code>&lt;my.thing /&gt;</code> 渲染
</div>
----

==== Element attributes
https://svelte.dev/docs/svelte/basic-markup#Element-attributes

[source,html]
----
<script>
    let p = $state(12);
    let disabled = $state(false);
</script>

<a class="text-blue-600 underline"
    // 1. 属性值里能包含 JS 表达式
    href="page/{p}"
>
    <!-- 2. 属性值可以是 JS 表达式 -->
    page {p}
</a>

<div class="mt-4 flex items-center gap-3">
    <!-- {disabled} 等价于 disabled={disabled} -->
    <button
        class="rounded bg-blue-600 px-3 py-1 text-white disabled:bg-gray-400"
        // 3. 属性名和它的值一样，则只写一个花括号包围的 JS 表达式就行了
        {disabled}
    >
        提交
    </button>
    <label class="flex items-center gap-1">
        <input type="checkbox" bind:checked={disabled} />
        测试禁用提交按钮
    </label>
</div>
----

